import React from 'react';
import './Button.css';

export default function Button(props) {
    return (
        <button id="spin" type="button" className="btn btn-color w-100px btn-lg ">
            Spin
        </button>
    )
}
